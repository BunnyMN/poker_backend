[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["cd packages/rules && npm install && npm run build && cd ../../apps/server && npm install && npm run build"]

[start]
cmd = "cd apps/server && npm start"
